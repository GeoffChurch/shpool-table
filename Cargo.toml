[package]
name = "shpool-manager"
version = "0.1.0"
edition = "2024"
description = "A TUI session manager that wraps shpool by shelling out to the shpool CLI."
license = "Apache-2.0"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
